// SocketException is only provided in dart:io. This empty implementation of
// will allow tests to compile and run on native + browser platforms.
class SocketException {
  SocketException(String message);
}
